name: test-vscode

on:
  push:
    branches:
      - main
      - dev
    paths:
      - "packages/neo-vscode/**"
      - "packages/ui/**"
      - "packages/neo-ui/**"
      - ".github/workflows/test-vscode.yml"
  pull_request:
    paths:
      - "packages/neo-vscode/**"
      - "packages/ui/**"
      - "packages/neo-ui/**"
      - ".github/workflows/test-vscode.yml"
  workflow_dispatch:

jobs:
  unit:
    name: unit tests
    runs-on: ubuntu-latest # neocode_change
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6 # neocode_change
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Bun
        uses: ./.github/actions/setup-bun

      - name: Run unit tests
        working-directory: packages/neo-vscode
        run: bun run test:unit

      - name: Check lint (ESLint)
        working-directory: packages/neo-vscode
        run: bun run lint

      - name: Check formatting (prettier)
        working-directory: packages/neo-vscode
        run: bun run format:check

      - name: Check for dead code (knip)
        working-directory: packages/neo-vscode
        run: bun run knip

      - name: Check for neocode_change markers
        working-directory: packages/neo-vscode
        run: bun run check-neocode-change
