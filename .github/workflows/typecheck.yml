name: typecheck

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  typecheck-js:
    name: typecheck-js
    runs-on: ubuntu-latest # neocode_change
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6 # neocode_change

      - name: Setup Bun
        uses: ./.github/actions/setup-bun

      # neocode_change start
      - name: Run TypeScript typecheck
        run: bun turbo typecheck --filter='!@neocode/neo-jetbrains'

      - name: Build Neo Console
        run: bun turbo build --filter=@neocode/neo-console
      # neocode_change end

  # neocode_change start
  jetbrains-changes:
    name: detect JetBrains changes
    runs-on: ubuntu-latest
    outputs:
      jetbrains: ${{ github.event_name == 'workflow_dispatch' && 'true' || steps.filter.outputs.jetbrains }}
    steps:
      - name: Checkout repository
        if: github.event_name != 'workflow_dispatch'
        uses: actions/checkout@v6

      - name: Detect JetBrains changes
        if: github.event_name != 'workflow_dispatch'
        id: filter
        uses: Neopilot-Ai/paths-filter@668c092af3649c4b664c54e4b704aa46782f6f7c # v3
        with:
          predicate-quantifier: every
          filters: |
            jetbrains:
              - '**'
              - '!.changeset/**'
              - '!packages/neo-vscode/**'
              - '!packages/neo-docs/**'

  typecheck-jetbrains:
    name: typecheck-jetbrains
    needs: jetbrains-changes
    if: github.event_name == 'workflow_dispatch' || needs.jetbrains-changes.outputs.jetbrains == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Bun
        uses: ./.github/actions/setup-bun

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          cache-read-only: ${{ github.ref != 'refs/heads/main' }}

      - name: Run JetBrains typecheck
        run: ./gradlew typecheck
        working-directory: packages/neo-jetbrains

  required:
    name: typecheck
    runs-on: ubuntu-latest
    needs:
      - typecheck-js
      - jetbrains-changes
      - typecheck-jetbrains
    if: always()
    steps:
      - name: Verify typecheck jobs passed
        run: |
          echo "typecheck-js=${{ needs.typecheck-js.result }}"
          echo "jetbrains-changes=${{ needs.jetbrains-changes.result }}"
          echo "typecheck-jetbrains=${{ needs.typecheck-jetbrains.result }}"
          test "${{ needs.typecheck-js.result }}" = "success"
          test "${{ needs.jetbrains-changes.result }}" = "success"
          if [ "${{ needs.jetbrains-changes.outputs.jetbrains }}" = "true" ]; then
            test "${{ needs.typecheck-jetbrains.result }}" = "success"
          else
            test "${{ needs.jetbrains-changes.outputs.jetbrains }}" = "false"
            test "${{ needs.typecheck-jetbrains.result }}" = "skipped"
          fi
  # neocode_change end
